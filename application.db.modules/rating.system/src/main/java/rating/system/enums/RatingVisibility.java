package rating.system.enums;

/**
 * The Enum RatingVisibility can give in which state the visibility of a rating is.
 */
public enum RatingVisibility {

	/** The rating is invisible. */
	INVISIBLE,
	/** The rating is approved and visible for others. */
	APPROVED,
	/** The rating is denied and not visible for others. */
	DENIED;

}
